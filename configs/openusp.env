# OpenUSP - Credentials and Sensitive Configuration
# This file contains only usernames, passwords, and other sensitive information
# All other configuration has been moved to openusp.yaml

# =============================================================================
# DATABASE CREDENTIALS
# =============================================================================
OPENUSP_DB_USER=openusp
OPENUSP_DB_PASSWORD=openusp123

# Alternative naming for compatibility
OPENUSP_POSTGRES_USER=openusp
OPENUSP_POSTGRES_PASSWORD=openusp123

# =============================================================================
# INFRASTRUCTURE SERVICE CREDENTIALS
# =============================================================================

# RabbitMQ Credentials
OPENUSP_RABBITMQ_USER=openusp
OPENUSP_RABBITMQ_PASSWORD=openusp123

# Grafana Admin Credentials
OPENUSP_GRAFANA_USER=openusp
OPENUSP_GRAFANA_PASSWORD=openusp123

## Removed agent and protocol client credentials (migrated to YAML: openusp.yml)
## Intentionally left blank placeholder section to avoid reintroducing via merges.

# =============================================================================
# LEGACY/COMPATIBILITY VARIABLES
# =============================================================================
# These variables are kept for backward compatibility with existing code
# that may still reference the old variable names

# Legacy database password references
OPENUSP_DB_PASSWORD=${OPENUSP_DB_PASSWORD}
OPENUSP_POSTGRES_PASSWORD=${OPENUSP_POSTGRES_PASSWORD}

# Legacy service password references  
OPENUSP_GRAFANA_PASSWORD=${OPENUSP_GRAFANA_PASSWORD}
OPENUSP_RABBITMQ_PASSWORD=${OPENUSP_RABBITMQ_PASSWORD}
OPENUSP_CWMP_PASSWORD=${OPENUSP_CWMP_PASSWORD}
